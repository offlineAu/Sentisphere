# railway.toml at repo root

[build]
builder = "DOCKERFILE"
dockerfilePath = "backend-dashboard/Dockerfile"

[deploy]
healthcheckPath = "/docs"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3